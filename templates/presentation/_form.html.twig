{{ form_start(form) }}
{{ form_row(form.pseudo,
  {'attr': {
        'class': 'my-5 margintop',
       ' row_attr':'form-floating',
       'placeholder': 'pseudo',
        }}
) }}
{{ form_row(form.email,
  {'attr': {
        'class': 'mb-4',
       ' row_attr':'form-floating',
       'placeholder': 'email',
        }}
) }}
    {{ form_row(form.submit,
    {'attr': {
        'class': 'btn-default',
        }}
    )
     }}
{{ form_end(form) }}