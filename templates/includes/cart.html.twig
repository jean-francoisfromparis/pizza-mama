	<h2>Panier</h2>
		<div class="card col-md-4">
			<div class="card-body">
				{% if carts | length>0 %}
					<table class="table table-striped table-hover">
						<thead>
							<tr>
								<th>Plat</th>
								<th>Prix</th>
								<th>Qté</th>
								<th>Total</th>
								<th></th>
							</tr>
						</thead>
						<tbody>
							{% for cart in carts %}
								<tr>
									<td>{{ cart.product.name }}</td>
									<td>{{ cart.product.price }}</td>
									<td>{{ cart.quantity }}</td>
									<td>{{ cart.quantity * cart.product.prix}}</td>
									<td>
										<a href=" {{path('supprimer_panier', {'id':panier.plat.id}) }} " class="btn btn-danger btn-sm" onclick="return confirm('⚠️ comfirmez vous la suppression de la ligne')">🗑️</a>
									</td>
								</tr>

							{% endfor %}
						</tbody>
						<tfoot>
							<tr>
								<td colspan="3" class="text-end">Total :
								</td>
								<td>
									{{ total }}
								</td>
								<td></td>
							</tr>
						</tfoot>
					</table>

				{% else %}
					<h4 class="mb-2 text-danger text-center">Vous n'avez encore rien mis dans votre panier</h4>
				{% endif %}


			</div>